{
  "name": "id3-js",
  "description": "A simple JavaScript library which can read ID3 tag from a audio file",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "rollup -c",
    "start": "rollup -c -w",
    "lint": "eslint --config eslint.config.js .",
    "test": "NODE_OPTIONS=--experimental-vm-modules jest"
  },
  "devDependencies": {
    "@rollup/plugin-terser": "^0.4.1",
    "eslint": "^8.38.0",
    "jest": "^29.5.0",
    "rollup": "^3.20.6"
  }
}
